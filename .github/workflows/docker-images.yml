name: 🐳 Docker Image Management

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        description: 'Environment to deploy to'
      web_image_name:
        required: true
        type: string
        description: 'Name of the web application Docker image'

jobs:
  validate-env:
    name: 🔍 Validate Environment Variables
    uses: ./.github/workflows/validate-env.yml
    with:
      required_vars: AWS_ACCESS_KEY_ID,AWS_SECRET_ACCESS_KEY,AWS_REGION,AWS_ACCOUNT_ID,DOCKER_REGISTRY,WEB_IMAGE_NAME
      job_name: docker-images

  docker-images:
    name: 🐳 Docker Image Management
    needs: validate-env
    runs-on: ubuntu-latest
    outputs:
      web_image_uri: ${{ steps.outputs.web_image_uri }}
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔐 Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 📦 Download Web Build
        uses: actions/download-artifact@v4
        with:
          name: angular-build-${{ inputs.environment }}
          path: dist

      - name: 🔑 Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🏗️ Build and Push Web Image
        id: web-image
        run: |
          # Set variables
          IMAGE_TAG="${{ inputs.environment }}-$(date +'%Y%m%d%H%M%S')"
          
          # Build the image
          docker build \
            -t ${{ inputs.web_image_name }}:$IMAGE_TAG \
            -t ${{ inputs.web_image_name }}:latest \
            -f services/octonius-web/Dockerfile \
            --build-arg NODE_ENV=${{ inputs.environment }} \
            .
          
          # Tag for ECR
          docker tag ${{ inputs.web_image_name }}:$IMAGE_TAG ${{ env.DOCKER_REGISTRY }}/${{ inputs.web_image_name }}:$IMAGE_TAG
          docker tag ${{ inputs.web_image_name }}:latest ${{ env.DOCKER_REGISTRY }}/${{ inputs.web_image_name }}:latest
          
          # Push to ECR
          docker push ${{ env.DOCKER_REGISTRY }}/${{ inputs.web_image_name }}:$IMAGE_TAG
          docker push ${{ env.DOCKER_REGISTRY }}/${{ inputs.web_image_name }}:latest
          
          # Set output
          echo "web_image_uri=${{ env.DOCKER_REGISTRY }}/${{ inputs.web_image_name }}:$IMAGE_TAG" >> $GITHUB_OUTPUT 